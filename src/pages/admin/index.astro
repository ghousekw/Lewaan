---
// Redirect to admin/index.html for local development
// On Netlify, the _redirects file handles this
---
<script>
  // Preserve hash tokens (invite/confirm/recovery) and avoid redirect loops.
  const isIndexHtml = window.location.pathname.endsWith('/admin/index.html');
  const hash = window.location.hash || '';
  if (!isIndexHtml) {
    window.location.replace(`/admin/index.html${hash}`);
  }
</script>
<noscript>
  <meta http-equiv="refresh" content="0; url=/admin/index.html">
</noscript>

